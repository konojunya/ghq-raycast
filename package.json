{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "ghq-vscode",
  "title": "GHQ VSCode",
  "description": "Control Visual Studio Code directly from Raycast - Search from ghq list and open project.",
  "icon": "command-icon.png",
  "author": "konojunya",
  "categories": [
    "System"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "Search Your Repositories",
      "description": "Search and open project from Visual Studio Code.",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "GHQ_ROOT_PATH",
      "title": "ghq root path",
      "description": "Your ghq root path.\nuse `ghq root` command to get the path.\n\nDO NOT use `~` to represent your home directory. Use the full path instead.",
      "type": "textfield",
      "required": true,
      "placeholder": "/User/username/ghq/src"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.67.1",
    "@raycast/utils": "^1.12.4"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.6",
    "@types/node": "20.8.10",
    "@types/react": "18.2.27",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
